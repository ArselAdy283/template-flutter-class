import 'package:flutter/material.dart';

class callPage extends StatelessWidget {
  const callPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("CALL"), backgroundColor: Colors.blue),
      body: 
        Center(
          child: Text("CALL"),
      ),
    );
  }
}